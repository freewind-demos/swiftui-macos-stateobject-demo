import SwiftUI

@main
struct StateObjectApp: App {
    var body: some Scene {
        Window("StateObject", id: "main") {
            ContentView()
        }
        .defaultSize(width: 400, height: 300)
    }
}